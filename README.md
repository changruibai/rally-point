# Rally Point - 最佳集合点应用

> 帮助多人找到最公平、最便捷的集合地点

---

## 📖 项目概述

### 产品定位
一个帮助多人（朋友聚会、团队出行、商务会面等场景）找到最佳集合点的 Web 应用。根据每个人的出发位置、交通方式，智能推荐一个让所有人出行时间都相对公平的集合地点。

### 核心价值
- **省时**：自动计算，无需反复商量
- **公平**：平衡每个人的出行成本
- **实用**：推荐真实可达的地点（咖啡厅、地铁站等）

---

## 🎯 功能需求

### P0 - MVP 核心功能

#### 1. 参与者管理
- 支持添加 2-8 个参与者
- 每个参与者可设置：
  - 出发地点（地址搜索/地图选点/当前位置）
  - 交通方式（步行/骑行/公交/驾车）
- 支持删除和编辑参与者

#### 2. 集合点计算
- 基于所有参与者位置，计算几何中心区域
- 在中心区域搜索 POI 候选点
- 对每个候选点计算所有人的到达时间
- 根据优化策略排序，推荐最佳集合点

#### 3. 结果展示
- 地图上显示：
  - 所有参与者的出发点（不同颜色标记）
  - 推荐的集合点（特殊标记）
  - 每个人到集合点的路线（不同颜色）
- 信息面板显示：
  - 集合点名称、地址
  - 每个人的预计到达时间
  - 总体时间统计

### P1 - 增强功能

#### 4. 集合点偏好
- 可选择集合点类型：
  - 🍽️ 餐厅
  - ☕ 咖啡厅
  - 🚇 地铁站
  - 🏬 商场/购物中心
  - 🅿️ 停车场

#### 5. 多方案对比
- 展示 Top 3 集合点方案
- 支持切换查看不同方案
- 显示每个方案的优劣对比

#### 6. 目的地考虑（可选）
- 可输入集合后的目的地
- 算法会优先推荐「在去目的地路上」的集合点

### P2 - 进阶功能

#### 7. 时间约束
- 可设置集合时间
- 根据时间计算实时路况

#### 8. 分享功能
- 生成分享链接
- 其他人可通过链接添加自己的位置

#### 9. 历史记录
- 保存常用出发地点
- 查看历史集合方案

---

## 🧠 算法设计

### 核心算法流程

```
输入：N个参与者的 (位置, 交通方式)
输出：最佳集合点 + 每人路线

1. 计算所有参与者的几何中心点
2. 以中心点为圆心，扩展搜索半径，获取 POI 候选点
3. 对每个候选点：
   - 调用路线规划 API，获取每人到达时间
   - 计算该点的「公平性得分」
4. 按得分排序，返回 Top N 结果
```

### 公平性得分计算

```typescript
// 策略1：最小化最大时间（公平优先）
function fairnessScore(times: number[]): number {
  return -Math.max(...times);
}

// 策略2：最小化总时间（效率优先）
function efficiencyScore(times: number[]): number {
  return -times.reduce((a, b) => a + b, 0);
}

// 策略3：平衡策略（默认）
function balancedScore(times: number[]): number {
  const max = Math.max(...times);
  const sum = times.reduce((a, b) => a + b, 0);
  const variance = calculateVariance(times);
  return -(0.4 * max + 0.3 * sum + 0.3 * variance * 10);
}
```

### 搜索优化

为避免 API 调用过多：
1. **分层搜索**：先用 500m 半径，如果候选点不足再扩大
2. **候选点上限**：最多取 10 个候选点进行详细计算
3. **缓存机制**：相同起终点的路线结果缓存 5 分钟

---

## 🗺️ 技术方案

### 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 框架 | Next.js 14 (App Router) | React 全栈框架 |
| 语言 | TypeScript | 类型安全 |
| 样式 | TailwindCSS | 原子化 CSS |
| 地图 | 高德地图 JS API 2.0 | 国内地图服务 |
| 路线 | 高德 Web 服务 API | 路线规划 |
| 状态 | Zustand | 轻量状态管理 |
| 部署 | Vercel | 自动部署 |

### 高德地图 API 使用

#### 1. JS API（前端地图展示）
```typescript
// 地图初始化
const map = new AMap.Map('container', {
  zoom: 13,
  center: [116.397428, 39.90923]
});

// 添加标记
const marker = new AMap.Marker({
  position: [lng, lat],
  title: '参与者1'
});
map.add(marker);

// 绘制路线
const polyline = new AMap.Polyline({
  path: routePath,
  strokeColor: '#3366FF',
  strokeWeight: 6
});
map.add(polyline);
```

#### 2. Web 服务 API（后端调用）

**地点搜索**
```
GET https://restapi.amap.com/v5/place/around
?key={key}
&location=116.473168,39.993015
&radius=1000
&types=050000|060000|080000  // 餐饮|购物|住宿
```

**路线规划（驾车）**
```
GET https://restapi.amap.com/v5/direction/driving
?key={key}
&origin=116.481028,39.989643
&destination=116.465302,40.004717
```

**路线规划（公交）**
```
GET https://restapi.amap.com/v5/direction/transit/integrated
?key={key}
&origin=116.481028,39.989643
&destination=116.465302,40.004717
&city=010
```

### API 路由设计

```
POST /api/calculate
  Body: { participants: [...], preferences: {...} }
  Response: { bestPoint: {...}, alternatives: [...] }

GET /api/search-poi
  Query: center, radius, types
  Response: { pois: [...] }

GET /api/route
  Query: origin, destination, mode
  Response: { duration, distance, path }
```

---

## 🎨 UI/UX 设计

### 整体布局

```
┌──────────────────────────────────────────────────────┐
│  Rally Point                              [计算]     │
├────────────────────┬─────────────────────────────────┤
│                    │                                 │
│   参与者输入面板    │                                 │
│   ──────────────   │                                 │
│   👤 参与者1       │                                 │
│   📍 朝阳区...     │         地图区域                │
│   🚗 驾车         │                                 │
│   ──────────────   │                                 │
│   👤 参与者2       │                                 │
│   📍 海淀区...     │                                 │
│   🚇 公交         │                                 │
│   ──────────────   │                                 │
│   [+ 添加参与者]   │                                 │
│                    │                                 │
│   ══════════════   │                                 │
│   推荐集合点       │                                 │
│   ──────────────   │                                 │
│   ☕ 星巴克(xxx店) │                                 │
│   参与者1: 23分钟  │                                 │
│   参与者2: 18分钟  │                                 │
│                    │                                 │
└────────────────────┴─────────────────────────────────┘
```

### 配色方案

```css
:root {
  /* 主色调 - 活力橙 */
  --primary: #FF6B35;
  --primary-light: #FF8F66;
  --primary-dark: #E55A2B;
  
  /* 背景色 */
  --bg-main: #FAFAFA;
  --bg-card: #FFFFFF;
  --bg-dark: #1A1A2E;
  
  /* 文字色 */
  --text-primary: #2D3436;
  --text-secondary: #636E72;
  
  /* 参与者颜色 */
  --participant-1: #3498DB;
  --participant-2: #E74C3C;
  --participant-3: #2ECC71;
  --participant-4: #9B59B6;
  --participant-5: #F39C12;
}
```

### 交互细节

1. **地点输入**
   - 输入时实时搜索建议
   - 支持点击地图选点
   - 支持获取当前位置

2. **计算过程**
   - 显示加载动画
   - 分步显示进度（搜索候选点 → 计算路线 → 排序结果）

3. **结果交互**
   - 悬停参与者高亮对应路线
   - 点击切换备选方案
   - 地图缩放适应所有标记

---

## 📁 项目结构

```
rally-point/
├── app/
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   ├── globals.css         # 全局样式
│   └── api/
│       ├── calculate/      # 计算集合点 API
│       │   └── route.ts
│       ├── search-poi/     # POI 搜索 API
│       │   └── route.ts
│       └── route/          # 路线规划 API
│           └── route.ts
├── components/
│   ├── MapView.tsx         # 地图组件
│   ├── ParticipantList.tsx # 参与者列表
│   ├── ParticipantCard.tsx # 单个参与者卡片
│   ├── LocationInput.tsx   # 地点输入组件
│   ├── ResultPanel.tsx     # 结果展示面板
│   └── TransportSelect.tsx # 交通方式选择
├── lib/
│   ├── amap.ts             # 高德地图封装
│   ├── algorithm.ts        # 核心算法
│   ├── api.ts              # API 请求封装
│   └── utils.ts            # 工具函数
├── store/
│   └── useAppStore.ts      # Zustand 状态管理
├── types/
│   └── index.ts            # TypeScript 类型定义
├── .env.local              # 环境变量（API Key）
├── package.json
├── tailwind.config.js
└── tsconfig.json
```

---

## 📊 数据类型定义

```typescript
// types/index.ts

/** 交通方式 */
type TransportMode = 'walking' | 'cycling' | 'transit' | 'driving';

/** 地理坐标 */
interface Coordinate {
  lng: number;
  lat: number;
}

/** 地点信息 */
interface Location {
  coordinate: Coordinate;
  address: string;
  name?: string;
}

/** 参与者 */
interface Participant {
  id: string;
  name: string;
  location: Location | null;
  transportMode: TransportMode;
  color: string;
}

/** POI 信息 */
interface POI {
  id: string;
  name: string;
  type: string;
  typeName: string;
  location: Location;
  distance?: number;
}

/** 路线信息 */
interface Route {
  participantId: string;
  duration: number;      // 分钟
  distance: number;      // 米
  path: Coordinate[];    // 路线坐标点
}

/** 集合点方案 */
interface MeetingPlan {
  poi: POI;
  routes: Route[];
  totalDuration: number;
  maxDuration: number;
  fairnessScore: number;
}

/** 计算请求 */
interface CalculateRequest {
  participants: Participant[];
  poiTypes?: string[];
  strategy?: 'fair' | 'efficient' | 'balanced';
}

/** 计算响应 */
interface CalculateResponse {
  bestPlan: MeetingPlan;
  alternatives: MeetingPlan[];
  searchCenter: Coordinate;
}
```

---

## 🚀 开发计划

### Phase 1: 基础搭建（Day 1）
- [x] 项目初始化（Next.js + TypeScript + TailwindCSS）
- [ ] 高德地图 JS API 集成
- [ ] 基础 UI 布局
- [ ] 地图组件封装

### Phase 2: 核心功能（Day 2-3）
- [ ] 参与者管理组件
- [ ] 地点搜索输入组件
- [ ] 交通方式选择组件
- [ ] 后端 API 路由

### Phase 3: 算法实现（Day 4）
- [ ] POI 搜索逻辑
- [ ] 路线规划调用
- [ ] 集合点评分算法
- [ ] 结果排序

### Phase 4: 结果展示（Day 5）
- [ ] 路线绑定
- [ ] 结果面板
- [ ] 多方案切换
- [ ] 交互优化

### Phase 5: 优化收尾（Day 6）
- [ ] 响应式适配
- [ ] 错误处理
- [ ] 加载状态
- [ ] 部署上线

---

## ⚙️ 环境配置

### 环境变量

```bash
# .env.local
AMAP_WEB_KEY=your_web_service_key    # 高德 Web 服务 Key
NEXT_PUBLIC_AMAP_JS_KEY=your_js_key  # 高德 JS API Key
NEXT_PUBLIC_AMAP_SECURITY_CODE=xxx   # 高德安全密钥
```

### 高德地图申请步骤

1. 访问 [高德开放平台](https://lbs.amap.com/)
2. 注册/登录账号
3. 进入控制台 → 应用管理 → 创建应用
4. 添加两个 Key：
   - **Web服务** Key（用于后端 API）
   - **Web端(JS API)** Key（用于前端地图）
5. 设置安全密钥（JS API 需要）

---

## 📝 注意事项

### 开发注意
1. 高德 API 有调用频率限制，开发时注意节流
2. 路线规划返回的坐标是高德坐标系（GCJ-02）
3. 公交路线规划需要指定城市编码

### 性能优化
1. 路线计算结果应该缓存
2. POI 搜索使用 debounce
3. 地图标记使用聚合显示（参与者多时）

### 用户体验
1. 提供默认参与者名称（参与者1、参与者2...）
2. 无结果时给出友好提示
3. 加载时显示骨架屏

---

## 🎯 成功指标

- 2人场景下，3秒内返回结果
- 5人场景下，8秒内返回结果
- 推荐结果的公平性偏差 < 30%（最长时间 / 最短时间）
- 用户完成一次完整操作 < 1分钟

---

*Let's Rally!* 🎯
